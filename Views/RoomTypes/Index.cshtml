@model IEnumerable<WebApplication1.Models.RoomType>

@{
    ViewData["Title"] = "Типы комнат";
}

<h1 style="color: #fffcea;">Типы комнат</h1>

<table class="table" style="margin-top: 20px;">
    <thead>
        <tr>
            <th style="color: #fffcea;">Название типа</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td style="color: #fffcea;">
                    @Html.DisplayFor(modelItem => item.name)
                </td>
            </tr>
        }
    </tbody>
</table>
